[package]
name = "ana-media"
description = "Video decode and encode for Ana-Convert, driving a bundled ffmpeg over pipes"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true

[dependencies]
ana-core = { path = "../ana-core" }
serde.workspace = true
serde_json.workspace = true
thiserror.workspace = true

[features]
# Lets sibling crates build the same test clips instead of duplicating the
# ffmpeg invocations. Off for normal builds.
fixtures = []

[dev-dependencies]
tempfile = "3"
